"use client";

// LogoAnimation component is no longer needed since logo is visible by default
// This component is kept for compatibility but does nothing
export default function LogoAnimation() {
  // Logo is now visible by default - no animation needed
  return null;
}
